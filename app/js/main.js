;(function($) {	'use strict';	var app = {		init: function () {			app.setUpListeners();			app.initModules();		},		initModules: function (){			$(window).on('load resize', function(){				app.backToTop();			});		},		setUpListeners: function () {		},		backToTop: function() {			var $toTop = $('.to-top-btn'),				stickyPos = $(document).height() - $(window).height() - $('.footer').height();			$(window).scroll(function() {				($(window).scrollTop() >= stickyPos) ? $toTop.addClass('to-top-btn_sticky') : $toTop.removeClass('to-top-btn_sticky');				($(this).scrollTop()) ? $toTop.stop().fadeIn(): $toTop.stop().fadeOut();			});			$toTop.bind('click', function() {				$('html, body').stop().animate({ scrollTop: 0 }, '500');				return false;			});		}	};	app.init();})(jQuery);